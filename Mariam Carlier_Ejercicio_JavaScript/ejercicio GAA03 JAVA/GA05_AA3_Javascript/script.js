let button = document.getElementById("b1");
let texto= document.getElementById("x1");

button.addEventListener("click", function() {
    // Seleccionamos TODOS los elementos con la clase "x1"
   texto.textContent = "Ahora cambió el texto"

    // Recorremos la lista y cambiamos uno por uno
});